---

groupmain:
  vars:
    ansible_connection: ssh
    ansible_user: todo--username-will-need-to-be-specified-per-device-groupings--not-here
  hosts:
    localhost:
      ansible_host: 127.0.0.1
      ansible_connection: local
    #######
    instance_nickname_1:
      ansible_host: XXXX.XXXX.XXXX.XXXX
    ####
    instance_nickname_2:
      ansible_host: XXXX.XXXX.XXXX.XXXX
    ####
    instance_nickname_3:
      ansible_host: XXXX.XXXX.XXXX.XXXX
    ####
    instance_nickname_4:
      ansible_host: XXXX.XXXX.XXXX.XXXX
    ####

...

# The following group names are in some playbooks in the comments as placeholders and if groups
# like this are created in the project's inventory, then these names should be used for consistency:
# groupcisco, groupcheckpoint, grouppaloalto, grouppanorama
#

